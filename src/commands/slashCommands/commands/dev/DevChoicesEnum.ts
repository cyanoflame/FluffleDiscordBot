/**
 * This enum establishes a common set of values that could be returned from the choices.
 */
export enum DevInfoChoice {
    ALL = "all",
    SYSTEM = "system",
    ENVIRONMENT = "environment",
    BOT = "bot"
};
